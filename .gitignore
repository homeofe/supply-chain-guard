node_modules/
dist/
*.tgz
.DS_Store
*.log
coverage/
.vitest/
.ai/logs/
# Scanner runtime output (self-scans, test-fixture scans). Never source.
.scg-history/
.scg-cache/
.claude/

# Local Claude Code / agent instructions - not project content, kept out of the repo
CLAUDE.md

# Superpowers skill planning docs - AI scratch, not project content
docs/superpowers/

# External model reviews - raw AI output, and this repo is public. Also keeps the
# daily job's `git add -A` from sweeping them into a commit.
docs/reviews/
