{
  "name": "scg-publish-toolchain",
  "private": true,
  "description": "The npm that publishes supply-chain-guard, installed by scripts/install-publish-npm.sh with npm ci so its bytes are checked against the integrity hash in package-lock.json.",
  "license": "Apache-2.0",
  "dependencies": {
    "npm": "11.18.0"
  }
}
