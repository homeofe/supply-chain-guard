# Keep lifecycle-script banners off npm pack stdout. The pre-commit
# framework installs this repo via "npm pack" and treats stdout as the
# tarball filename; with foreground scripts the "prepare" banner breaks
# that parsing. Script output is still shown on failure.
foreground-scripts=false
